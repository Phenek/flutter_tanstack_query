name: tanstack_query
description: Widgets that help state management of asynchronous operations such as sending HTTP requests, getting the response, and caching the result.
version: 1.2.4
repository: https://github.com/Phenek/flutter_tanstack_query/tree/main/packages/tanstack_query

environment:
  sdk: ">=2.19.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  async: ^2.10.0
  flutter_hooks: ^0.21.2
  meta: ^1.17.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  flutter_lints: ^6.0.0
  test: ^1.26.3
  internet_connection_checker_plus: ^2.9.1+2
